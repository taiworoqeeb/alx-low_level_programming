#include <stdio.h>
/**
 * main - Print an output using printf
 * Return: 0 if code runs properly
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
