#include "main.h"
/**
 * mul- function to multiply two numbers
 * @a: first integer to be multiplied
 * @b: multiplier
 *
 * Return: the answer
 */
int mul(int a, int b)
{
	int c;

	c = a * b;
	retutn(c);
}
